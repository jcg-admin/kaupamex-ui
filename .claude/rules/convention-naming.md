# Convention naming — cheat-sheet (canónico en docs)

Regla completa: `docs/.claude/rules/convention-naming.md` — se carga en sesiones con
`docs` en scope. Aquí solo el invariante operativo (Opción B, iniciativa
`consolidar-reglas-fuente-unica`, DEC-01/02):

Kebab-case, sin prefijos numéricos en nombres de archivo; números solo en IDs (UC-001) y timestamps.
